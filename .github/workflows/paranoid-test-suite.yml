name: 💀 Paranoid Test Suite (0.01% Error Rate)

on:
  push:
    branches: [ main, develop, feature/* ]
  pull_request:
    branches: [ main, develop ]
  workflow_dispatch: # Manual trigger

# Cancel in-progress runs for same branch (PRs only)
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

permissions:
  contents: read
  security-events: write  # Required for CodeQL/Trivy SARIF upload

env:
  PYTHON_VERSION: '3.11'
  NODE_VERSION: '20'
  QDRANT_URL: http://localhost:6333

jobs:
  # ==========================================
  # 🔍 JOB 0: Pre-flight Checks
  # ==========================================
  preflight:
    name: 🔍 Pre-flight Checks
    runs-on: ubuntu-latest
    outputs:
      backend_changed: ${{ steps.changes.outputs.backend }}
      frontend_changed: ${{ steps.changes.outputs.frontend }}
      
    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🔍 Detect Changes
        id: changes
        uses: dorny/paths-filter@v3
        with:
          filters: |
            backend:
              - 'backend/**'
              - '.github/workflows/**'
            frontend:
              - 'frontend/**'
              - '.github/workflows/**'

      - name: 📊 Change Summary
        run: |
          echo "🔹 Backend changed: ${{ steps.changes.outputs.backend }}"
          echo "🔹 Frontend changed: ${{ steps.changes.outputs.frontend }}"

  # ==========================================
  # 🧪 JOB 1: RAG Ingestion Tests
  # ==========================================
  rag-ingestion-tests:
    name: 📦 RAG Ingestion Tests
    runs-on: ubuntu-latest
    needs: preflight
    if: needs.preflight.outputs.backend_changed == 'true'
    
    services:
      qdrant:
        image: qdrant/qdrant:latest
        ports:
          - 6333:6333
          - 6334:6334
        # Use default healthcheck from Dockerfile (curl not available in Alpine container)

    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v4

      - name: 🐍 Setup Python ${{ env.PYTHON_VERSION }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'
          cache-dependency-path: backend/requirements.txt

      - name: 📦 Install Dependencies
        run: |
          cd backend
          pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest pytest-cov pytest-timeout numpy

      - name: 🏥 Wait for Qdrant to be ready
        run: |
          echo "⏳ Waiting for Qdrant to be ready..."
          
          for i in {1..60}; do
            if curl -f -s http://localhost:6333/health > /dev/null 2>&1; then
              echo "✅ Qdrant is ready after $i seconds"
              curl -s http://localhost:6333/health | head -5
              exit 0
            fi
            echo "⏳ Attempt $i/60: Waiting for Qdrant..."
            sleep 1
          done
          
          echo "❌ Qdrant failed to start after 60 seconds"
          echo "Debug info:"
          curl -v http://localhost:6333/health || true
          exit 1

      # Run RAG ingestion tests with Qdrant service
      - name: 🧪 Run RAG Ingestion Tests
        env:
          QDRANT_URL: ${{ env.QDRANT_URL }}
        run: |
          cd backend
          pytest tests/test_rag_ingestion.py \
            -v \
            --timeout=120 \
            -m "not slow" \
            --tb=short \
            -x

      # Integration tests skipped - see team decision in expert_meeting_transcript_mode2.md
      - name: 🔗 Run Integration Tests (Requires Qdrant)
        run: |
          echo "Integration tests skipped - GitHub Actions resource constraints"
          echo "Run locally with: pytest tests/test_rag_ingestion.py -m integration"
          exit 0

      - name: 📊 Upload Coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          files: ./backend/coverage-ingestion-unit.xml,./backend/coverage-ingestion-integration.xml
          flags: rag-ingestion
          name: rag-ingestion-coverage
          fail_ci_if_error: false
        env:
          CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}

  # ==========================================
  # 🧪 JOB 2: Backend API Tests
  # ==========================================
  backend-api-tests:
    name: 🐍 Backend API Tests
    runs-on: ubuntu-latest
    needs: preflight
    if: needs.preflight.outputs.backend_changed == 'true'
    
    strategy:
      fail-fast: false
      matrix:
        python-version: ['3.11']

    services:
      qdrant:
        image: qdrant/qdrant:latest
        ports:
          - 6333:6333

    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v4

      - name: 🐍 Setup Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: 'pip'

      - name: 📦 Install Dependencies
        run: |
          cd backend
          pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest pytest-cov pytest-asyncio pytest-timeout httpx

      - name: 🧪 Run Backend Tests
        env:
          QDRANT_URL: ${{ env.QDRANT_URL }}
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY != '' && secrets.GROQ_API_KEY || 'test_key_for_ci' }}
        run: |
          cd backend
          pytest tests/ \
            -v \
            --tb=short \
            --cov=app \
            --cov-report=xml:coverage-backend-py${{ matrix.python-version }}.xml \
            --cov-report=term-missing \
            --timeout=60

      - name: 📊 Upload Coverage
        uses: codecov/codecov-action@v4
        with:
          files: ./backend/coverage-backend-py${{ matrix.python-version }}.xml
          flags: backend-py${{ matrix.python-version }}
          name: backend-py${{ matrix.python-version }}
          fail_ci_if_error: false
        env:
          CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}

  # ==========================================
  # 🧪 JOB 3: Frontend Tests
  # ==========================================
  frontend-tests:
    name: ⚛️ Frontend Tests
    runs-on: ubuntu-latest
    needs: preflight
    # Run frontend tests on all pushes to catch UI bugs early
    if: needs.preflight.outputs.frontend_changed == 'true'
    
    strategy:
      fail-fast: false
      matrix:
        node-version: ['20']

    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v4

      - name: 📦 Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: 📦 Install Dependencies
        run: |
          cd frontend
          npm ci --prefer-offline --no-audit

      - name: 🧪 Run Frontend Tests
        run: |
          cd frontend
          npm test -- --run --reporter=verbose

      - name: 📊 Upload Coverage
        if: matrix.node-version == '20'
        uses: codecov/codecov-action@v4
        with:
          files: ./frontend/coverage/coverage-final.json
          flags: frontend-node${{ matrix.node-version }}
          name: frontend-node${{ matrix.node-version }}
          fail_ci_if_error: false
        env:
          CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}

  # ==========================================
  # 🏗️ JOB 4: Build Verification
  # ==========================================
  build-check:
    name: 🏗️ Build Verification
    runs-on: ubuntu-latest
    needs: preflight
    
    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v4

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: 📦 Install Frontend Dependencies
        run: |
          cd frontend
          npm ci

      - name: 🏗️ Build Frontend
        run: |
          cd frontend
          npm run build

      - name: 📊 Build Size Check
        run: |
          cd frontend/dist
          SIZE=$(du -sh . | cut -f1)
          echo "📦 Build size: $SIZE"
          
          # Check if build is too large (> 5MB)
          SIZE_BYTES=$(du -sb . | cut -f1)
          if [ $SIZE_BYTES -gt 5242880 ]; then
            echo "⚠️ Warning: Build size is large ($SIZE)"
          else
            echo "✅ Build size is acceptable"
          fi

      - name: 📤 Upload Build Artifact
        uses: actions/upload-artifact@v4
        with:
          name: frontend-build
          path: frontend/dist/
          retention-days: 7

  # ==========================================
  # 🔒 JOB 5: Security Scan
  # ==========================================
  security-scan:
    name: 🔒 Security Scan
    runs-on: ubuntu-latest
    needs: preflight
    
    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v4

      - name: 🔍 Run Trivy Vulnerability Scanner
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'fs'
          scan-ref: '.'
          format: 'sarif'
          output: 'trivy-results.sarif'
          severity: 'CRITICAL,HIGH'

      - name: 📤 Upload Trivy Results to GitHub Security
        uses: github/codeql-action/upload-sarif@v3
        if: always()
        with:
          sarif_file: 'trivy-results.sarif'

  # ==========================================
  # 📊 JOB 6: Test Summary & Reporting
  # ==========================================
  test-summary:
    name: 📊 Test Summary
    runs-on: ubuntu-latest
    needs: [rag-ingestion-tests, backend-api-tests, frontend-tests, build-check]
    if: always()
    
    steps:
      - name: 📊 Generate Summary
        run: |
          echo "# 💀 Paranoid Test Suite Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## Test Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Component | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|-----------|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| RAG Ingestion | ${{ needs.rag-ingestion-tests.result == 'success' && '✅' || needs.rag-ingestion-tests.result == 'skipped' && '⏭️' || '❌' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Backend API | ${{ needs.backend-api-tests.result == 'success' && '✅' || needs.backend-api-tests.result == 'skipped' && '⏭️' || '❌' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Frontend | ${{ needs.frontend-tests.result == 'success' && '✅' || needs.frontend-tests.result == 'skipped' && '⏭️' || '❌' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Build Check | ${{ needs.build-check.result == 'success' && '✅' || '❌' }} |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "---" >> $GITHUB_STEP_SUMMARY
          echo "🎯 **Target Error Rate:** 0.01%" >> $GITHUB_STEP_SUMMARY

  # ==========================================
  # ✅ JOB 7: All Tests Passed Gate
  # ==========================================
  all-tests-passed:
    name: ✅ All Tests Passed
    runs-on: ubuntu-latest
    needs: [rag-ingestion-tests, backend-api-tests, frontend-tests, build-check, security-scan]
    if: always()
    
    steps:
      - name: 🎉 Check All Tests Status
        run: |
          if [[
            "${{ needs.rag-ingestion-tests.result }}" == "success" &&
            "${{ needs.backend-api-tests.result }}" == "success" &&
            "${{ needs.frontend-tests.result }}" == "success" &&
            "${{ needs.build-check.result }}" == "success" &&
            "${{ needs.security-scan.result }}" == "success"
          ]]; then
            echo "🎉 All tests passed! Ready to merge."
            exit 0
          else
            echo "❌ Some tests failed. Please fix before merging."
            exit 1
          fi
